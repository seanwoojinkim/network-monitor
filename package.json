{
  "name": "network-scanner",
  "version": "1.0.0",
  "description": "Local network device scanner",
  "main": "electron-main.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "electron": "electron .",
    "package": "electron-packager . 'Network Scanner' --platform=darwin --arch=x64 --out=dist --overwrite --icon=icon.icns",
    "package-arm": "electron-packager . 'Network Scanner' --platform=darwin --arch=arm64 --out=dist --overwrite --icon=icon.icns"
  },
  "dependencies": {
    "bonjour": "^3.5.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "node-arp": "^1.0.6",
    "node-ssdp": "^4.0.1",
    "ping": "^0.4.4"
  },
  "devDependencies": {
    "electron": "^27.0.0",
    "electron-packager": "^17.1.2"
  }
}
